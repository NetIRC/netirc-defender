# Runtime, secrets, and local policy files — do not commit
data
defender.conf
defender.log
defender.pid
defender_persistent_counters.v1
seen_state.sto
checkdefender
checkD.log
nohup.out
.checkD.lock

# Local ircd snippets often contain secrets
ircd.conf

# Per-install config (copy from *.example in repo)
dnsbl.conf
regexp_akill.conf
deny_version.conf
glines.conf
killchans.conf

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*~
